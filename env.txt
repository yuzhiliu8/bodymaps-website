#This is a template document for a .env file to make docker-compose work
#Each variable needs to have a value, or else deployment will not work
#CLIENT

VITE_API_ORIGIN=
VITE_BASENAME=


#SERVER
SERVER_BASE_PATH=
